class Item {
  static all = [];

  constructor(id, name, price, item_type, toppings) {
    this.id = id;
    this.name = name;
    this.price = price;
    this.item_type = item_type;
    this.toppings = toppings;
  }
}
